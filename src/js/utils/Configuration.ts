export default {
  // Path needs to be absolute because it's used for images, inline CSS but also variables read from the compiled CSS
  imagePath: '/assets/' as string,
  openOverlayWhenInnerWidthGreatherThan: 1500 as number,
}
